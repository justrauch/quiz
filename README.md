start MySQL in XAMPP (Admin Mode) (start HeidiSQL for that Instanz)
set up db in /server with python .\db.py
start docker in /server with docker-compose up --build
start react in /quiz with npm run dev
