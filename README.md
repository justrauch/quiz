start MySQL in XAMPP (Admin Mode) (start HeidiSQL for that Instanz) <br>
set up db in /server with python .\db.py <br>
start Docker Desktop App or Demon in Console <br>
start docker in /server with docker-compose up --build <br>
start react in /quiz with npm run dev <br>
